/**
 * Animate element into view.
 * @param {string|Element} target - Selector or element.
 * @param {Object} options - Animation options.
 */
export default function animateIn(target, options = {}) {
  // TODO: Implement animate in
  console.log('animateIn initialized on', target);
}
