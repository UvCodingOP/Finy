/**
 * Animate element out of view.
 * @param {string|Element} target - Selector or element.
 * @param {Object} options - Animation options.
 */
export default function animateOut(target, options = {}) {
  // TODO: Implement animate out
  console.log('animateOut initialized on', target);
}
